{
  "name": "shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and utilities for SAD diesel machine booking platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --write \"src/**/*.{ts,tsx}\""
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.8.2",
    "eslint": "^9",
    "prettier": "^3.2.5",
    "rimraf": "^6"
  }
}
